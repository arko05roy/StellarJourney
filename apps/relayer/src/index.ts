/**
 * Untrusted relayer entrypoint (BullMQ worker that builds, simulates, and
 * submits charge invocations). Populated starting Phase 9. Placeholder
 * only in Phase 0 — exists so the workspace builds and typechecks.
 */
export function placeholder(): string {
  return "@paymap/relayer";
}
