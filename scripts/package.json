{
  "name": "@paymap/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "echo \"no build step: CLI scripts run directly via tsx\" && exit 0",
    "test": "echo \"no tests yet\" && exit 0",
    "deploy:testnet": "tsx deploy-testnet.ts",
    "demo:mandate": "tsx create-demo-mandate.ts",
    "demo:relayer": "tsx run-relayer-testnet-demo.ts"
  },
  "dependencies": {
    "@paymap/contract-client": "workspace:*",
    "@paymap/relayer": "workspace:*",
    "@paymap/shared": "workspace:*",
    "@paymap/stellar": "workspace:*",
    "@stellar/stellar-sdk": "^16.1.0"
  },
  "devDependencies": {
    "@paymap/config": "workspace:*",
    "tsx": "^4.23.1"
  }
}
