/**
 * Generated Soroban client bindings for mandate-registry, plus a typed
 * wrapper (i128 <-> bigint, never `number`). Populated starting Phase 7.
 * Empty in Phase 0.
 */
export const CONTRACT_CLIENT_PACKAGE_NAME = "@paymap/contract-client" as const;
