/**
 * Shared UI components (shadcn/ui-based) used by apps/web's consumer and
 * merchant surfaces. Populated starting Phase 10. Empty in Phase 0.
 */
export const UI_PACKAGE_NAME = "@paymap/ui" as const;
