/**
 * Merchant-facing TypeScript SDK (checkout sessions, charges, refunds,
 * webhook verification). Populated starting Phase 12. Empty in Phase 0.
 */
export const SDK_PACKAGE_NAME = "@paymap/sdk" as const;
