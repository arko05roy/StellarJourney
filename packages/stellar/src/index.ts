/**
 * Soroban transaction builder, simulation helper, auth-entry assembly, and
 * contract error-code decoder. Populated starting Phase 7. Empty in Phase 0.
 */
export const STELLAR_PACKAGE_NAME = "@paymap/stellar" as const;
